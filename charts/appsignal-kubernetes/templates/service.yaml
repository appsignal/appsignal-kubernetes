apiVersion: v1
kind: Service
metadata:
  name: {{ include "appsignal-kubernetes.fullname" . }}
  labels:
    {{- include "appsignal-kubernetes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appsignal-kubernetes.selectorLabels" . | nindent 4 }}
