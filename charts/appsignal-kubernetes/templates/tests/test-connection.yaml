apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appsignal-kubernetes.fullname" . }}-test-connection"
  labels:
    {{- include "appsignal-kubernetes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appsignal-kubernetes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
